import { Screen, Text } from '@/components';

export default function Favorites() {
  return (
    <Screen
      headerProps={{
        title: 'My Favorites',
      }}
    >
      <Text>Favorites screen</Text>
    </Screen>
  );
}
