import { Screen, Text } from '@/components';

export default function Show() {
  return (
    <Screen
      headerProps={{
        title: 'Show',
      }}
    >
      <Text>Show screen</Text>
    </Screen>
  );
}
